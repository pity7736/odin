info:
  name: list chunks unauthorized
  type: http
  seq: 13

http:
  method: GET
  url: "{{host}}/chunks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
