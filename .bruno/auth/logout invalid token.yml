info:
  name: logout invalid token
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{host}}/auth/logout"
  headers:
    - name: Authorization
      value: Bearer not-a-real-token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
