info:
  name: logout
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{host}}/auth/logout"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
