info:
  name: login wrong credentials
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "some@email.com",
        "auth_hash": "wrong-password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
